import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        ezoko: {
          green: '#4ADE80',
          dark: '#0f1115',
        }
      },
    },
  },
};
export default config;